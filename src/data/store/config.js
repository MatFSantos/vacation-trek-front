// Arquivo de configuração que contém todas as configurações da aplicação.

const API_URL = "http://localhost:3333";

const snackbarError = {variant: 'error', anchorOrigin: {horizontal:'right', vertical: 'top'}, autoHideDuration: 5000};

const snackbarSuccess = {variant: 'success', anchorOrigin: {horizontal:'right', vertical: 'top'}, autoHideDuration: 5000};

export { API_URL, snackbarError, snackbarSuccess };